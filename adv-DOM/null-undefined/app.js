/**
 * 
 * undefined = variable where value is not assigned
 * 
 * null = represents a non-existant value
 * 
 */

// declared varaible that is not yet assigned
let age;
console.log(age);

let food = null;
console.log(food);
